#include <iostream>
#include <vector>
#include <string>

using namespace std;

// functions use the "function call stack"
// push and pop, (LIFO - last in first out)
// when a function finishes, we pop the activation record (stack frame) and return to caller
// local var and function param are allocated on the stack
// heap are another class of memory known as free store
int main() {
    

    return 0;
}
