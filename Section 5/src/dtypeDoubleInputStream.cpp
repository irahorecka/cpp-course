#include <iostream>
using namespace std;

int main() {
    double dub;

    // read a double from the input stream - define double
    // dtype first.
    cout << "Input a double: ";
    cin >> dub;
    cout << "You put in the value " << dub << endl;
}